#!/usr/bin/make -f

%:
	dh $@ --with autoreconf

override_dh_auto_configure:
	dh_auto_configure -- --disable-libewf --quiet

override_dh_auto_build:
	dh_auto_build -- CXXFLAGS="$(CXXFLAGS) -Wno-error=date-time"
