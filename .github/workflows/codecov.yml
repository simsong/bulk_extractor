# Just run for codecov

name: Codecov

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04

    env:
      DEBUG_5G: FALSE
      DEBUG_FAST: TRUE
      MAKE_OPTS: -j2

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Install Ubuntu dependencies
        run: |
          source "$HOME/.bashrc"
          echo "" | bash etc/CONFIGURE_UBUNTU22LTS.bash

      - name: Make configure script
        run: |
          bash bootstrap.sh

      - name: C++ checks with codecov
        run: |
          source "$HOME/.bashrc"
          ./configure --disable-opt --enable-silent-rules --quiet \
                CFLAGS='-g -O0 -fprofile-arcs -ftest-coverage' \
                CXXFLAGS='-g -O0 -fprofile-arcs -ftest-coverage'
          make $MAKE_OPTS check
          lcov --capture --directory . --output-file linux-coverage.info
          lcov --remove linux-coverage.info '/usr/*' \
                --output-file linux-coverage.filtered.info
          lcov --list linux-coverage.filtered.info

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          disable_search: true
          fail_ci_if_error: true
          files: linux-coverage.filtered.info
          flags: unittests
          name: bulk_extractor-codecov
          override_pr: ${{ github.event.pull_request.number }}
