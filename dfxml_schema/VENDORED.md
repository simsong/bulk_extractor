# Integrated source provenance

The DFXML schema was imported as ordinary tracked source on 2026-07-19 from `https://github.com/dfxml-working-group/dfxml_schema.git` at commit `f2a702e401dd15802ec8558b42c496a1184a0486`.

The upstream license and attribution files remain in this directory. The schema is distributed with bulk_extractor and no longer requires a separate Git submodule checkout.
